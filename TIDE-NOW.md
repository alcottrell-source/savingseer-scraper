# Tide — Ground Truth Status

**Date:** 2026-07-05
**Source of truth:** current repo file contents + `git log`/`git blame` on branch `claude/tide-ground-truth-status-9o6d64` (HEAD `ffd12e6`). Prose docs (CLAUDE.md, and any external May-2026 engineering-state doc or 5 Jul 2026 positioning doc) are **not** authoritative — where they conflict with code, code wins and the conflict is named below. The two external docs referenced in the task brief are not present in this repo, so they could not be inspected directly; only their claims as relayed in the task brief are checked against code.

## Section A — Resolved contradictions

| # | Item | Verdict | Evidence |
|---|------|---------|----------|
| 1 | Scraper | **OUT** | `scraper.js` does not exist (`ls scraper.js` → not found). Deleted in commit `5cbc5a6` ("Remove the scraper; admin user-reports become the review nudge", #133, 2026-06-21), which also stripped scraper fallbacks from `supabase/functions/notify-high-tide/index.ts` and dropped the 06:00 crawl job from `.github/workflows/daily-scrape.yml`. That workflow file still exists (name unchanged) but its only job (`score`) runs `node score.js` then `node summarise.js` — no scraper invocation (`.github/workflows/daily-scrape.yml:1-33`). `.github/workflows/e2e.yml` mentions the string "scraper" only as part of the repo's own hostname in a preview-URL regex (`e2e.yml:29,58`), not a scraper process. No sale-state read path remains: `index.html:6104` and `score.js:424` each carry a comment stating `sale_status` is deliberately **not** read; grepping the two files for `sale_status` returns only those comments, no live reads. Sale state is purely admin-verified cycles (`brand_sale_cycles` / `active_cycle_id`). |
| 2 | Affiliate | **NONE (live network) / a plain outbound CTA is live** | Skimlinks was explicitly removed from `index.html` and `privacy.html` in commit `5e6125b` ("Remove Skimlinks affiliate tracker", merged as `9e906bb`, #113, 2026-05-31; −78 lines in `index.html`, −10 in `privacy.html`). No `<script>` tag for Skimlinks, Awin, or any affiliate network exists anywhere in `index.html` today (only script include is Supabase JS, `index.html:38`). The shop-detail sheet's `"See it on {brand} →"` CTA (`index.html:6759`) links straight to `BRAND_URL_BY_NAME[name]` (`index.html:3091` onward — plain retailer URLs, e.g. `'Next':'https://www.next.co.uk/'`, no tracking params, no redirect domain) and still shows the note **"Tide may earn a commission. You never pay more."** (`index.html:6760`) even though no affiliate network is wired — this is a real inconsistency in the current code, not a stale doc, flagged here rather than silently reconciled. Separately, `vercel.json`'s CSP still allowlists `https://s.skimresources.com`/`https://*.skimresources.com` in both `script-src` and `connect-src` (`vercel.json:15`) — dead config left over from the removed integration, since nothing loads that domain. |
| 3 | Fonts | **Playfair Display + Inter only, in shipped `index.html`** | `index.html:28` loads exactly `Playfair+Display:ital,wght@0,400;0,600;0,700;1,400` and `Inter:wght@400;500;600;700` from Google Fonts — no other family. `body` sets `font-family: 'Inter', sans-serif` (`index.html:77`); display elements use `'Playfair Display', serif` (e.g. `index.html:81,91,155,156,160`). No occurrence of "DM Sans" or "Cormorant" anywhere in `index.html`. Caveat: **DM Sans is real elsewhere in the repo** — `admin.html:10` and `privacy.html:9` both load and use `DM+Sans` (paired with Playfair) — so a doc describing the admin console or privacy page, not the public app, would correctly say DM Sans. For the shipped consumer app (`index.html`) the answer is unambiguously Playfair + Inter. |
| 4 | Onboarding steps | **5 steps, not 4** | `openOnboarding()` (`index.html:2084`) sets `onb.step = 1` and `obRender()` (`index.html:2135-2142`) dispatches steps 1–5 to `obRenderStep1` → `obRenderStep2` → `obRenderStep3` → `obRenderStep4Centres` → `obRenderStep5Notifications`. Order/content, each confirmed by its own "Step N of 5" eyebrow string in the code: (1) audiences — "Who are you shopping for?" (`index.html:2155-2156`); (2) categories — "What do you actually buy?" (`index.html:2177-2178`); (3) brands — "Your brands" (`index.html:2246-2247`); (4) centres — "Where do you shop?" (`index.html:2288-2289`); (5) notifications + live personal-score preview (`index.html:2308` onward). **This directly contradicts CLAUDE.md itself** (`CLAUDE.md:48`: *"4-step wizard: gender → style clusters → notifications + saved centres → preview"*) — CLAUDE.md is stale on this specific point; the centre-selection step (4) is a distinct step from notifications (5), not merged into one as CLAUDE.md describes. |

## SEO v1 status (item 5 — reported, not a contradiction)

| Check | Status | Evidence |
|---|---|---|
| `sitemap.xml` generated by the build | **Yes** | `seo/generate.mjs:271` writes `sitemap.xml` into the build output dir; the file is assembled from every generated centre/brand/blog URL (`seo/generate.mjs:262-276`). |
| Referenced in Vercel config | **Yes** | `vercel.json:3` sets `"buildCommand": "node seo/generate.mjs"`, so it runs on every Vercel build. `robots.txt` (repo root) points crawlers at `Sitemap: https://tidego.co/sitemap.xml`. |
| `seo/generate.mjs` wired into the build | **Yes** | Same `vercel.json:3` `buildCommand` entry — it's the entire build step (`outputDirectory` is `.`, no separate bundler). |
| `seo_alert_signups` migration exists | **Yes** | `supabase/migrations/20260603_add_seo_alert_signups.sql` (present in repo, dated 2026-06-03 by filename) creates the table with RLS: anon insert-only, admin read. |

Whether these have actually been *applied/run in production* (migration executed against the live DB, Vercel build succeeding with the real env vars, pages actually indexed) is out of repo — see Section C.

## Section B — Shipped since 2026-05-30

Non-merge commits, newest first, `2026-05-30`–`2026-07-05` on the current branch/history:

- `ffd12e6` 07-03 Move My shops/All shops toggle to sit above the shop list on centre pages (#164)
- `baf545e` 07-03 Remove inert My shops/All shops toggle from the landing page (#163)
- `7214c68` 07-02 Restore global "My shops / All shops" view-mode selector (#160)
- `7f66741` 07-02 Fix search placeholder truncation on mobile (#161)
- `e3922c3` 07-02 Chart: default to 30D and float endpoint pills clear of the line (#159)
- `ea73b33` 07-02 Prompt users they can search by town, city or postcode (#158)
- `a90d452` 07-02 Nearest-centre search by place name or postcode (#157)
- `17aac53` 07-01 Centre hero: drop card title + row markers, shrink metric rows (#156)
- `e3ba796` 07-01 Rebuild centre hero to two-column metric layout (#155)
- `c4dfc8a` 06-30 Endpoint pills: fixed order — your-shops above, whole-centre below (#154)
- `ac093b7` 06-30 Fix overlapping chart endpoint pills; differentiate the two (#153)
- `3623d85` 06-30 Per-line weekly movement under each headline % (#152)
- `aadf1c3` 06-30 Centre hero redesign: one movement signal, one clock; legend back to a key (#151)
- `e67f0d8` 06-29 Make chart key + movement pop: bold HTML legend above the chart
- `dccc4b6` 06-29 Legend badge: lead with shop-count change, percentage in parens
- `9b600a1` 06-29 Move tide up/down signal onto the chart legend
- `b7d2161` 06-29 Add movement chip to centre hero (shop-count + score-points change) (#149)
- `b8f496b` 06-28 Clarify your-shops headline copy and add brand icons to newest sales
- `1a4aa1b` 06-28 Lead centre hero with dual percentages: your shops over whole centre (#148)
- `964c339` 06-28 Tidy tide chart: recede whole-centre line, soften green haze
- `10387cc` 06-28 Share a branded tide card image, not just text (#146)
- `e6f1f53` 06-28 seo: skip thin brand pages (off-sale, no history) to lift index rate (#145)
- `42eb7c8` 06-27 Strengthen whole-centre chart line and show both shop percentages (#144)
- `4dbf35a` 06-27 Design pass: brand convergence, hero copy discipline, consistent chart encoding (#143)
- `0149b7f` 06-26 Keep whole-centre verdict ("Go now") visible in Your-shops mode
- `90862bc` 06-26 Rework centre hero: view toggle drives headline, count line, and hero chart line
- `380d854` 06-26 Landing: lead with all-centres chart, drop the 'Go now' sample card
- `60ca9c2` 06-26 Move all-centres line chart to top of landing page
- `ecc4651` 06-26 Deep-link brand-sale alerts into the personal centre view (KR3)
- `5cd21f3` 06-26 Replace toggle with toggle-free 'your shops' personalisation (KR1/2/4)
- `85da908` 06-26 Make 'My shops' the personalisation spine across the journey
- `842eff2` 06-26 Add 'your shops' personal line to the centre history chart
- `0c7ede6` 06-26 Move amber chart dot from peak-entry to peak crest
- `97d942b` 06-26 Fix silent row-cap truncation that froze `tide_history` since ~June 1
- `ea98ce9` 06-26 Make centre-detail history chart x-axis proportional to calendar date
- `7c0e3e2` 06-24 Fix duplicate brand-sale alert (re-confirm re-armed the one-shot email) (#140)
- `e83f3d6` 06-22 Blog: reading the Tide card at high tide (Westfield London) (#139)
- `ea2840d` 06-22 Geo-aware expansion waitlist + admin view + signup notifier (#137)
- `aae9506` 06-22 Add Westfield London showcase card to main page hero (#136)
- `f75028f` 06-21 Fix: define `verdictLabel` so opening a centre doesn't throw (#135)
- `136da68` 06-21 SEO: harden indexing — root meta, branded 404, sitemap lastmod
- `1547ee6` 06-21 Add growth loop + blog: shareable centre links, social previews, SEO linking, referrals (#129)
- `608c931` 06-21 Make Newest Sales discount text + hero count pop in brand green (#134)
- **`5cbc5a6` 06-21 Remove the scraper; admin user-reports become the review nudge (#133)**
- `319e315` 06-21 Remove On-sale-now box; list all newest sales
- `8089bcb` 06-21 Centre chart: date-based 7D/30D/60D/MAX tabs (default 60D) + 180d retention (#132)
- `3554d38` 06-21 Brand-strip logos + restore 7D/30D/MAX centre-chart tabs (#131)
- `3e94792` 06-21 Home "Today's tide" rebuild + centre-chart redundant-tab fix (#130)
- `285cfce`–`9f77425` 06-21 Centre chart: fixed 0–100% axis, gridlines, drop PEAK/QUIET lines, amber peak-hit dots
- `147cc14` 06-16 Fix: peak email sends every day a centre stays at Peak
- `ef61ca3` 06-15 Add Edit button to every admin shop card (#126)
- `ebf59fb` 06-15 Move signed-out sign-up CTA below the value content
- `35121af` 06-15 Make all-centres 60-day card dark-themed and larger
- `e8aa7aa` 06-15 feat(admin): surface public-site rescore failures instead of failing silently (#125)
- `5167ac7` 06-15 fix(landing): anchor all-centres 60-day average curve to live data (#124)
- `fa1edf8` 06-15 fix(score): self-heal stored score on the daily cron (force fresh recompute) (#123)
- `9a63be4` 06-15 fix(tide-line): anchor 60-day chart's today point to the live headline value (#122)
- `2f6cd14` 06-15 fix(tide-line): stop synthetic past cresting above today's real peak (#121)
- `6a70ee1` 06-15 fix(seo): stop phantom/mass 404s and fix thin-content not-indexed pages (#120)
- `839042b`–`905e3fd` 06-13/14 feat(detail-sheet): add Tide shop detail bottom sheet, wire Alert-me CTA, iterate sale-rhythm chart shape
- `12cea8d` 06-12 feat(seo): add Bing Webmaster Tools site verification file
- `c743347` 06-12 feat(seo): add robots.txt with sitemap pointer
- `4e69a03` 06-12 fix(seo): write `sitemap.xml` not `sitemap-seo.xml` so `/sitemap.xml` resolves in production
- `e1b7020` 06-12 Add Google site verification meta tag
- `201bd15` 06-10 Add logos/links for new brands (Bershka, JD Sports, Sports Direct, Footasylum, Urban Outfitters, Victoria's Secret, Pull&Bear, Diesel) (#118)
- `a4e2443` 06-04 Expand SEO to all active centres + consent-gated GA4/Clarity (#117)
- `52e288d` 06-04 Replace Plausible with consent-gated Google Analytics 4 (#116)
- `db76d74`/`79627a8`/`139861a`/`3643f85`/`84ca359` 06-03 feat(seo): programmatic SEO engine v1 (WestQuay) — brand pages, breadcrumb JSON-LD, resilient build, public-anon-key generation (#115)
- `b5537c1` 05-31 chore: remove stale cookie-banner items from regression checklist
- **`5e6125b` 05-31 Remove Skimlinks affiliate tracker (#113)**
- `b4c4d2d` 05-31 Hot list: rank "Today's tide" by verdict severity then score (#112)
- `1e876db` 05-31 TIDE funnel redesign: pain-led hero, brand-led funnel copy (#111)

**Themes:** scraper removal + admin-verified-only sale state (06-21); Skimlinks removal (05-31); SEO engine v1 stood up then hardened (06-03 → 06-28, ongoing); centre-hero encoding rebuilt repeatedly (dual %, toggle removed then restored, two-column metric layout) through late June/early July; chart axis/gridline/dot-marker correctness fixes (06-15, 06-21); score/chart self-heal fixes for stale `tide_history` (06-15, 06-26); nearest-centre postcode search added (07-02).

## Section C — Confirm with Alex (out of repo)

- Vercel project env vars actually set (`SUPABASE_URL`, `SUPABASE_SERVICE_KEY`) — not visible from the repo, only that `score.js`/`vercel.json`/`/api/rescore.js` expect them.
- Whether `GEMINI_API_KEY` is populated as a GitHub Actions secret for the `daily-scrape.yml` `score` job — the workflow references `secrets.GEMINI_API_KEY` (soft-fail if absent) but secret values aren't in the repo.
- Whether `SUPABASE_ACCESS_TOKEN` is set for `.github/workflows/deploy-functions.yml` (Edge Function deploys).
- Google Search Console indexing state for the SEO v1 pages — repo only shows the generator, sitemap, robots.txt and verification files exist; actual indexing/crawl status is external.
- Bing Webmaster Tools verification status (verification file exists at `12cea8d`, but confirmation of it being live in Bing's console is external).
- Any Facebook/social post results referenced in outside conversations — no trace of these in the repo.
- Whether the brand-sale/peak-alert email probe has actually run/been observed firing in production — code exists (`supabase/functions/notify-high-tide`) but runtime execution isn't repo-visible.
- Content and claims of the external "May-2026 engineering-state doc" and "5 Jul 2026 product-positioning doc" — neither file is present in this repository, so their specific wording couldn't be checked line-by-line against code; only the contradictions named in the task brief were checked.
